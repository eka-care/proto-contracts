syntax = "proto3";

package vault.features;

option go_package = "github.com/eka-care/proto-contracts/go/vault/features";

enum Feature {
  TYPE_UNSPECIFIED = 0;  // Unspecified.
  TYPE_INSURANCE = 1;    // Insurance.
  TYPE_MY_HEALTH = 2;    // My Health.
  TYPE_INSIGHTS = 3;     // Insights.
  TYPE_SECRET_VAULT = 4; // Secret Vault.
}